services:
  callq-department1:
    image: callq:latest
    container_name: callq-department1
    network_mode: host
    env_file:
      - .env.department1
    environment:
      - JSON_AUTH=${JSON_AUTH}
    volumes:
      - ./logs:/app/logs
      - ./prompts:/app/prompts:ro
    restart: "no"
