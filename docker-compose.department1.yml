services:
  callq-department1:
    build:
      context: .
    container_name: callq-department1
    network_mode: host
    env_file:
      - .env.department1
    volumes:
      - ./logs:/app/logs
      - ./acc_google_api:/app/acc_google_api:ro
      - ./prompts:/app/prompts:ro
    restart: "no"
