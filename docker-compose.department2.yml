services:
  callq-department2:
    build:
      context: .
    container_name: callq-department2
    network_mode: host
    env_file:
      - .env.department2
    environment:
      - JSON_AUTH=${JSON_AUTH}
    volumes:
      - ./logs:/app/logs
      - ./prompts:/app/prompts:ro
    restart: "no"
