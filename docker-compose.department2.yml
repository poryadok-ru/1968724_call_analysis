services:
  callq-department2:
    build: .
    container_name: callq-department2
    dns:
      - 8.8.8.8
      - 1.1.1.1
    env_file:
      - .env.department2
    volumes:
      - ./logs:/app/logs
      - ./acc_google_api:/app/acc_google_api:ro
      - ./prompts:/app/prompts:ro
    restart: "no"
